import { Inter, Lusitana } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})
export const lusitana = Lusitana({
  subsets: ['latin'],
  variable: '--font-read',
  weight: '700',
})
